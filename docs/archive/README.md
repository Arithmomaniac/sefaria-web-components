> Created/edited by GitHub Copilot; pending human review.

# Documentation archive

The current starting point is the [documentation home](../README.md). This directory preserves historical material, not another supported set of commands or contracts.

## September 6, 2026 snapshot

`2026-09-06/` is an exact copy of the root README, `docs/` tree including images, and text-transform package README at repository commit `7f631685961b3ff6243d812570e1ffbd85aa53c6`, before the reader-facing rewrite. It was captured before editing, from Git rather than from an active feature worktree.

The snapshot retains the original relative directory layout and the four Showboat demo transcripts. Its `README.md`, `docs/`, and `packages/` paths are historical paths. Links to implementation files not included in the snapshot must be resolved against that commit, not treated as missing current features.

Do not update or reformat the snapshot. Its original wording, status claims, timestamps, and human-review disclaimers are historical records, not fresh assertions. Follow the current [specifications](../README.md#detailed-reference), [development guide](../development.md), and [evidence record](../evidence.md) for maintained information.

## What remains active?

Specifications, architecture ownership, and source evidence remain at their established current paths. The rewrite adds reader-oriented guides; it does not retire valid contracts or silently replace deliberate local behavior with Sefaria defaults.

Old demo capture pages remain reachable at their former locations through short pointers to the current guide and this archive. New prose should link to current guides, not treat a dated capture as an API reference.
