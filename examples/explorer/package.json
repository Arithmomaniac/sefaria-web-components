{
  "name": "@sefaria-example/explorer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@sefaria/client": "workspace:*",
    "@sefaria/web-components": "workspace:*",
    "lit": "^3.3.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build",
    "dev": "pnpm --workspace-root build && pnpm --workspace-root exec vite . --open /examples/explorer/",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json"
  }
}
