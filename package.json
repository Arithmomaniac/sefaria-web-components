{
  "name": "sefaria-web-components-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "build:mcp": "pnpm --filter @sefaria-demo/mcp-app build && pnpm stage:mcp",
    "check": "pnpm openapi:check && pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm compatibility:qualify && pnpm build && pnpm check:python:staged",
    "check:python": "pnpm install:python && pnpm --filter @sefaria-demo/mcp-app build && pnpm check:python:staged",
    "check:python:staged": "pnpm stage:mcp && uv run --no-sync --directory demos/mcp/fixture-server ruff format --check . && uv run --no-sync --directory demos/mcp/fixture-server ruff check . && uv run --no-sync --directory demos/mcp/fixture-server mypy src tests && uv run --no-sync --directory demos/mcp/fixture-server pytest",
    "client:fixture:capture-candidate": "pnpm --filter @sefaria/client fixture:capture-candidate",
    "compatibility:qualify": "pnpm --filter @sefaria-tests/compatibility qualify",
    "dev": "pnpm --filter @sefaria-demo/component-lab dev",
    "dev:linker": "pnpm --filter @sefaria-demo/linker-userscript dev",
    "dev:mcp": "pnpm build:mcp && uv run --no-sync --directory demos/mcp/fixture-server sefaria-mcp-app-fixture",
    "dev:ref-label": "pnpm --filter @sefaria-demo/ref-label-live-demo dev",
    "dev:text-segment": "pnpm --filter @sefaria-demo/text-segment-live-demo dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "install:python": "uv venv --allow-existing --directory demos/mcp/fixture-server && uv pip sync --directory demos/mcp/fixture-server requirements.lock",
    "lint": "eslint .",
    "openapi:check": "pnpm --filter @sefaria/client openapi:check",
    "openapi:generate": "pnpm --filter @sefaria/client openapi:generate",
    "openapi:refresh": "pnpm --filter @sefaria/client openapi:refresh",
    "stage:mcp": "node demos/mcp/stage-fixture.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "@vitest/browser-playwright": "^4.1.10",
    "eslint": "^10.0.1",
    "eslint-plugin-jsdoc": "62.9.0",
    "playwright": "^1.62.1",
    "prettier": "^3.9.6",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vite": "^8.2.1",
    "vite-plugin-monkey": "^8.1.0",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.10",
    "vitest-browser-lit": "^1.0.1"
  }
}
